{
  "name": "nexora-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter \"./**\" build",
    "test": "jest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
